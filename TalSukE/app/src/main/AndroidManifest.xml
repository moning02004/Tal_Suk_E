<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.talsuke">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".InfoActivity" />
        <activity android:name=".LocalActivity" />
        <activity android:name=".MeActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />

        <activity android:name=".InfoClickedActivity" />
        <activity android:name=".InfoEditActivity" />
        <activity android:name=".SettingRegDelActivity" />
        <activity android:name=".SettingRegInfoActivity"></activity>
        <activity android:name=".InfoDetailActivity" />
        <activity android:name=".InfoEditActivity"></activity>
    </application>

</manifest>